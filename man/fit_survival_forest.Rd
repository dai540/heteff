% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_survival_forest.R
\name{fit_survival_forest}
\alias{fit_survival_forest}
\title{Fit a Heterogeneous Survival Effect Forest}
\usage{
fit_survival_forest(
  data,
  time,
  status,
  treatment,
  covariates,
  target = "RMST",
  horizon = NULL,
  num.trees = 2000,
  seed = 1,
  ...
)
}
\arguments{
\item{data}{A data frame containing all required columns.}

\item{time}{Name of observed time column.}

\item{status}{Name of event indicator column (1 = event, 0 = censored).}

\item{treatment}{Name of treatment column.}

\item{covariates}{Character vector of baseline covariate column names.}

\item{target}{Prediction target passed to \code{grf::predict()}.}

\item{horizon}{Optional horizon passed to \code{grf::predict()}.}

\item{num.trees}{Number of trees passed to \code{grf::causal_survival_forest()}.}

\item{seed}{Random seed passed to \code{grf::causal_survival_forest()}.}

\item{...}{Additional arguments passed to \code{grf::causal_survival_forest()}.}
}
\value{
A \code{heteff_fit} object.
}
\description{
Wraps \code{grf::causal_survival_forest()} with a column-name interface and
computes out-of-bag effect predictions through \code{predict()}.
}
\examples{
\dontrun{
set.seed(2)
n <- 300
df <- data.frame(
  time = rexp(n, 0.2),
  status = rbinom(n, 1, 0.8),
  trt = rbinom(n, 1, 0.5),
  x1 = rnorm(n),
  x2 = rnorm(n)
)
fit <- fit_survival_forest(
  data = df,
  time = "time",
  status = "status",
  treatment = "trt",
  covariates = c("x1", "x2"),
  target = "RMST",
  horizon = 5
)
head(as.data.frame(fit))
}
}
