% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_instrumental_forest.R
\name{fit_instrumental_forest}
\alias{fit_instrumental_forest}
\title{Fit a Heterogeneous Instrumental-Variable Forest}
\usage{
fit_instrumental_forest(
  data,
  outcome,
  treatment,
  instrument,
  covariates,
  num.trees = 2000,
  seed = 1,
  ...
)
}
\arguments{
\item{data}{A data frame containing all required columns.}

\item{outcome}{Name of outcome column.}

\item{treatment}{Name of exposure or treatment column.}

\item{instrument}{Name of instrument column.}

\item{covariates}{Character vector of baseline covariate column names.}

\item{num.trees}{Number of trees passed to \code{grf::instrumental_forest()}.}

\item{seed}{Random seed passed to \code{grf::instrumental_forest()}.}

\item{...}{Additional arguments passed to \code{grf::instrumental_forest()}.}
}
\value{
A \code{heteff_fit} object.
}
\description{
Wraps \code{grf::instrumental_forest()} with a column-name interface.
}
\examples{
\dontrun{
set.seed(3)
n <- 400
z <- rbinom(n, 1, 0.5)
x1 <- rnorm(n)
x2 <- rnorm(n)
w <- 0.6 * z + 0.4 * x1 + rnorm(n)
y <- 1.2 * w + 0.5 * x2 + rnorm(n)
df <- data.frame(y = y, w = w, z = z, x1 = x1, x2 = x2)

fit <- fit_instrumental_forest(
  data = df,
  outcome = "y",
  treatment = "w",
  instrument = "z",
  covariates = c("x1", "x2")
)
head(as.data.frame(fit))
}
}
