% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_observational_forest.R
\name{fit_observational_forest}
\alias{fit_observational_forest}
\title{Fit a Heterogeneous Effect Forest for Observational Data}
\usage{
fit_observational_forest(
  data,
  outcome,
  treatment,
  covariates,
  num.trees = 2000,
  seed = 1,
  ...
)
}
\arguments{
\item{data}{A data frame containing all required columns.}

\item{outcome}{Name of the numeric outcome column.}

\item{treatment}{Name of the binary or continuous treatment column.}

\item{covariates}{Character vector of baseline covariate column names.}

\item{num.trees}{Number of trees passed to \code{grf::causal_forest()}.}

\item{seed}{Random seed passed to \code{grf::causal_forest()}.}

\item{...}{Additional arguments passed to \code{grf::causal_forest()}.}
}
\value{
A \code{heteff_fit} object with \code{effect_table}, fitted forest, and
variable importance.
}
\description{
Wraps \code{grf::causal_forest()} with a column-name interface and returns a
unified \code{heteff_fit} object.
}
\examples{
\dontrun{
set.seed(1)
n <- 300
df <- data.frame(
  y = rnorm(n),
  w = rbinom(n, 1, 0.5),
  x1 = rnorm(n),
  x2 = rnorm(n)
)
fit <- fit_observational_forest(df, "y", "w", c("x1", "x2"))
head(as.data.frame(fit))
}
}
