# Export output tables to CSV files

Export output tables to CSV files

## Usage

``` r
export_tables(fit, output_dir)
```

## Arguments

- fit:

  A `heteff_fit` object.

- output_dir:

  Directory to write outputs into.

## Value

Invisibly returns the output directory.
