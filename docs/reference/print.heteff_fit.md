# Print a `heteff_fit` summary

Print a `heteff_fit` summary

## Usage

``` r
# S3 method for class 'heteff_fit'
print(x, ...)
```

## Arguments

- x:

  A `heteff_fit` object.

- ...:

  Unused.

## Value

The input object, invisibly.
